<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataTemplate x:Key="OutgoingConnectionView">
        <Border Background="DarkSlateBlue">
            <StackPanel Margin="0,2,0,4">
                <ItemsControl ItemsSource="{Binding Conditions}" AlternationCount="1000">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock FontWeight="Bold" HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Else If"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ContentPresenter}, Path=(ItemsControl.AlternationIndex)}" Value="0">
                                                    <Setter Property="Text" Value="If"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="180" Margin="2"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,2,0,0">
                    <Button Content="-" Width="20" Height="20" Margin="2" Background="DarkGray"
                            Command="{Binding DataContext.RemoveOutgoingConnectionCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}"/>
                    <Button Content="Conditions"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Command="{Binding AddConditionCommand}"
                            Margin="0,0,4,0"/>
                    <Button Content="Outgoing"
                            HorizontalAlignment="Right"
                            Command="{Binding AddOutgoingConnectionCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </DataTemplate>
</ResourceDictionary>